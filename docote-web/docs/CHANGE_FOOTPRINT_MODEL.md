# Change Footprint Model

## Purpose
DoCoTe should summarize the rough shape of a change set, not just its inferred documentation impact.

## Current step
Add a lightweight change-footprint model that classifies changed files into docs-like and code-like buckets.

## Why this matters
This helps users quickly understand whether a run is primarily about code churn, documentation churn, or a mix of both.
